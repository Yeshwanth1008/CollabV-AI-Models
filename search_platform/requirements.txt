# Semantic User Search & Recommendation Platform (port 8002) — see
# ARCHITECTURE.md. Note: fastapi/uvicorn are also in the root
# requirements.txt (shared with collabv/); listed here too so this
# subsystem's dependencies are complete and installable on their own.
fastapi==0.110.0
uvicorn==0.27.0
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
pgvector==0.5.0
sentence-transformers==5.1.0
torch==2.8.0
numpy==1.26.0
anthropic==0.69.0
rank-bm25==0.2.2
RapidFuzz==3.14.5
python-dotenv==1.0.0
pydantic==2.11.7
pydantic-settings==2.10.1
