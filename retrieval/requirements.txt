# Faculty Information Retrieval System (port 8001) — see SETUP.md.
# Note: fastapi/uvicorn are also in the root requirements.txt (shared with
# collabv/); listed here too so this subsystem's dependencies are complete
# and installable on their own.
fastapi==0.110.0
uvicorn==0.27.0
rank-bm25==0.2.2
scikit-learn==1.4.0
scipy==1.12.0
requests==2.31.0
aiohttp==3.9.0
