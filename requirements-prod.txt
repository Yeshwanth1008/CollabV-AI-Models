# CollabV AI - production-only dependencies (install ALONGSIDE requirements.txt)
#
# Usage:
#   pip install -r requirements.txt -r requirements-prod.txt

# Dense embeddings + vector search
sentence-transformers>=2.5.0
faiss-cpu>=1.7.4
torch>=2.0.0

# PostgreSQL async + pgvector + migrations
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.2.4
alembic>=1.13.0

# Pydantic email types
pydantic[email]>=2.5.0
email-validator>=2.1.0

# Cache / queue (optional)
redis>=5.0.1
celery>=5.3.6

# Auth
bcrypt>=4.1.2
PyJWT>=2.8.0

# Monitoring
sentry-sdk[fastapi]>=1.40.0
